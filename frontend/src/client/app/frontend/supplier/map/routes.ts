import { Route } from '@angular/router';
import { SupplierMapComponent } from './component';

export const SupplierMapRoutes: Route[] = [
    {
        path: 'supplierMap',
        component: SupplierMapComponent
    }
];
