import { Component, OnInit } from '@angular/core';

import { MdlIconComponent } from 'angular2-mdl';

@Component({
    moduleId: module.id,
    selector: 'sd-app-nav',
    styleUrls: ['styles.css'],
    templateUrl: 'tmpl.html'
})

export class NavigationComponent implements OnInit {

    constructor() { }

    ngOnInit() {
    }
}
