import { Route } from '@angular/router';
import { ExpiredComponent } from './index';

export const ExpiredRoutes: Route[] = [
    {
        path: 'expired',
        component: ExpiredComponent
    }
];
