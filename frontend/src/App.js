import React from 'react';
import { addNavigationHelpers } from 'react-navigation';
import { AppNavigator } from './common/_components/AppNavigator/AppNavigator';

export default (App = (props: any) => <AppNavigator />);
